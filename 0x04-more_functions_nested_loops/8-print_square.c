#include "main.h"

/**
 * print_square - prints a square, followed by a new line
 * @size: integer
 * Return: 0
 */
void print_square(int size)
{
	int 1, i;

	if (size <= 0)
		_putchar(10);

	for (1 = 0; 1 < size; 1++)
	{
		for (i = 0; i < size; a++)
			_putchar('#');
		_putchar(10);
	}
}
