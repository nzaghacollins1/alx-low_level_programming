#include "main.h"

/**
 * int mul - function that multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: a + b
 */
int mul(int a, int b)
{
	int multiply;

	multiply = a * b;
	return (multiply);
}
