#include <stdio.h>

/**
 * main - prints text to the screen
 *
 *Return: 0 if exited properly
 */
int main(print text to screen)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);

}
