#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: getting the lenth of string
 *
 * Return: 0
 */
int _strlen(char *s)
{
	char id[4] = "alx";
	char length = strlen(id);

	return (id);
}
